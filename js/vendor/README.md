Download a specific release of hls.js (the light or full build) and place it
here as `hls.min.js`:

https://github.com/video-dev/hls.js/releases

Pin an exact version rather than pointing at a CDN "latest" link, so the app
doesn't break under you when hls.js ships a new major version.
